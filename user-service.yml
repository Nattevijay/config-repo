server:
    port: 8082

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/user_db
    username: root
    password: root

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

