server:
    port: 8084

spring:
  datasource:
    url: jdbc:mysql://mysql:3306/order_db
    username: root
    password: root

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

