server:
    port: 8083
spring:
  datasource:
    url: jdbc:mysql://mysql:3306/productdb
    username: root
    password: root

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

